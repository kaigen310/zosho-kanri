# 📚 蔵書管理 PWA

蔵書・読書状況を管理し、AIに「次に読む1冊」を聞くためのプロンプトをワンクリック生成するツールのPWA版。

- サーバー不要。データはブラウザのlocalStorageにのみ保存される（このリポジトリに個人データは含まれない）
- iPhoneのSafariで開いて「ホーム画面に追加」するとアプリとして使える（オフライン対応）
- バックアップ・端末間の引っ越しは「データ」タブのJSONエクスポート/インポートで行う
- データ形式はMac版（Flask + `data/books.json`）と互換
